# ============================================================
# CADD一站式研发平台 — 依赖环境
# 目标环境: Python 3.12.2 / Windows 11 / Streamlit 1.37.1
# ============================================================

# ---- 核心框架 ----
streamlit>=1.37.0,<1.38.0
protobuf>=4.25.0,<5.0.0          # streamlit / tensorboard 兼容

# ---- 化学信息学 ----
rdkit>=2024.03.1                  # 分子解析、描述符、QED、2D渲染

# ---- 数据与数值计算 ----
pandas>=2.0.0,<2.3.0
numpy>=1.24.0,<2.0.0             # 锁定 <2.0，避免大量生态包报错
scipy>=1.11.0                     # sklearn 内部依赖
openpyxl>=3.1.0                   # pd.read_excel .xlsx 支持

# ---- 机器学习 ----
scikit-learn>=1.3.0,<1.6.0
xgboost>=2.0.0,<3.0.0

# ---- 深度学习 (SMILESGPT) ----
torch>=2.3.0,<2.6.0               # Win + Python 3.12 需 ≥ 2.3
transformers>=4.40.0,<5.0.0       # AutoModelForCausalLM, AutoTokenizer

# ---- 可视化 ----
plotly>=5.17.0,<6.0.0
py3Dmol>=2.0.0                    # 蛋白质 3D 结构渲染
stmol>=0.0.9                      # streamlit 中嵌入 py3Dmol

# ---- 分子对接 (子进程隔离运行, 避免与 torch 的 C++ 库冲突) ----
meeko>=0.5.0                      # 受体 PDB → PDBQT 准备
vina>=1.2.0                       # AutoDock Vina Python 绑定
prody>=2.4.0                      # meeko PDB 解析后端之一
gemmi>=0.6.0                      # meeko PDB 解析后端之二

# ---- 外部 API / 数据源 ----
requests>=2.28.0                  # PubChem / UniProt / Open Targets / NCBI
zhipuai>=2.0.0                    # 智谱 GLM-4 智能助手

# ---- 图像处理 (RDKit MolToImage 依赖) ----
Pillow>=10.0.0
